{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --forceExit",
    "test:win": "set NODE_OPTIONS=--experimental-vm-modules && jest --forceExit", 
    "test:watch": "set NODE_OPTIONS=--experimental-vm-modules && jest --watch",
    "test:debug": "set NODE_OPTIONS=--experimental-vm-modules && jest --detectOpenHandles"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.20.0",
    "mongoose": "^8.18.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "jest": "^30.1.3",
    "supertest": "^7.1.4"
  }
}
